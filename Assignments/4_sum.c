//Experiment 4 - Read two numbers, add them and display their sum.
#include <stdio.h>

// Add two numbers
void main(){
    int a, b;
    printf("Input two numbers: ");
    scanf("%d %d", &a, &b);
    printf("Sum: %d\n", (a+b));
}